{
  "name": "pokedex",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "browser-sync start --server --files \"./components/*.html\" --port 8080",
    "fake:api": "json-server --watch db.json"
  },
  "devDependencies": {
    "browser-sync": "^2.24.6",
    "json-server": "^0.14.0"
  }
}
